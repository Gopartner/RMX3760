name: Build TWRP RMX3760 Android 15

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    permissions:
      contents: write

    steps:
      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          df -h

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git-core repo gnupg flex bison build-essential zip curl \
            zlib1g-dev libc6-dev-i386 lib32z1-dev lib32ncurses6 libncurses5-dev \
            libxml2-utils xsltproc unzip python3 python-is-python3 rsync cpio lz4 bc

      - name: Prepare repo
        run: |
          mkdir -p ~/bin
          ln -sf /usr/bin/repo ~/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      # 1. Checkout Device Tree dulu ke folder sementara (Aman dari wipe)
      - name: Checkout Device Tree
        uses: actions/checkout@v4
        with:
          path: device_tree_temp

           # 2. Init & Sync TWRP Manifest Source (Diperbarui ke cabang twrp-14)
      - name: Init TWRP Source
        run: |
          mkdir -p workspace
          cd workspace
          git config --global user.name "arriRgb31"
          git config --global user.email "febyyusuf12345@gmail.com"
          repo init --depth=1 -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-14
          repo sync -c --force-sync --no-clone-bundle --no-tags -j$(nproc)

            # 3. Pindahkan Device Tree ke lokasi AOSP setelah repo sync tuntas
      - name: Setup Device Tree Location
        run: |
          mkdir -p workspace/device/realme/RMX3760
          cp -r device_tree_temp/* workspace/device/realme/RMX3760/
          rm -rf device_tree_temp

      # 4. Process Build murni untuk target init_boot (PERBAIKAN DI SINI)
      - name: Process Build init_boot
        run: |
          cd workspace
          export ALLOW_MISSING_DEPENDENCIES=true
          export LC_ALL=C
          source build/envsetup.sh
          # FORMAT BARU: Menambahkan parameter release trunk_staging khas Android 14/15
          lunch twrp_RMX3760-trunk_staging-eng
          # Kompilasi resmi menggunakan target init_bootimage
          mka init_bootimage -j$(nproc)

      # 5. Collect & Verify Artifacts
      - name: Collect & Verify Artifacts
        run: |
          mkdir -p workspace/output
          # Jalur out wajib diselaraskan dengan hasil lunch trunk_staging
          if [ -f workspace/out/target/product/twrp_RMX3760/init_boot.img ]; then
            cp workspace/out/target/product/twrp_RMX3760/init_boot.img workspace/output/twrp_init_boot.img
            echo "SUCCESS: init_boot.img berhasil dibuat!"
          else
            echo "ERROR: init_boot.img TIDAK DITEMUKAN DI JALUR BUILD!"
            exit 1
          fi

